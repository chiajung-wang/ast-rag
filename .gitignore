# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Env
.env
.env.local

# Index (large binary, rebuild with make index)
*.db
*.db-shm
*.db-wal

# Corpus (cloned separately, not committed)
langchain/
corpus/

# Evals output (generated, commit manually when publishing results)
evals/results.md
evals/results/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# AI
.claude
skills-lock.json

# Personal
.personal

# Retrieval eval embedding cache (regenerated, costs ~nothing)
evals/.embedding_cache.json

# Judge validation key — must stay hidden from the human labeler until
# scoring, and the blind file itself (labels are the whole point of A3).
evals/.judge_validation_key.jsonl
evals/judge_validation_blind.jsonl
evals/judge_validation.jsonl
